/*const express = require("express");
const app = express();
const PORT = 5000;

app.listen(PORT, () => {
    console.log("Server Is Running On port " + PORT);
})

 this is only containing the server running but data is not fetching or we are not getting any data from databse */



/*const express = require("express");
const app = express();
const PORT = 5000;
const mongoose = require("mongoose");
// i am accessing url from keys.js module
const mongoURL = require("./keys");

mongoose.connect(mongoURL);
mongoose.connection.on("connected", () => {
    console.log("connected");
})
mongoose.connection.on("Error", () => {
    console.log("Error");
})

app.listen(PORT, () => {
    console.log("Server Is Running On port " + PORT);
})
 in this code we are using mongoose library to connect the data to database and in this file we are checking both connection and error
to create database we are using models folder in backend which is used to create schema  */


//where ever we are using mongo db we have to import mongoose library